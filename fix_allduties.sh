sed -i 's/const allDuties = DUTY_TYPES.filter((dt) => dt.isCountedAsDuty);/const _storedMatrix = getStoredDutyMatrix(); const allDuties = DUTY_TYPES.filter((dt) => { if (!dt.isCountedAsDuty) return false; const mEntries = _storedMatrix.filter(t => t.dutyCode === dt.code); if (mEntries.length > 0 \&\& mEntries.every(t => t.isDisabled)) return false; return true; });/g' src/components/AssignDutyModal.tsx
